# include<stdio.h>

# include<string.h>

/* WAP to compute length of a string by using In-Built functions. */

void main()
{
    char list[] = "Welcome to Programming World.";

    int len = strlen(list);

    printf("The length of the string is : %d",len);
}
